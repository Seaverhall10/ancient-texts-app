# Session Update — March 7, 2026

## What Was Done This Session

### 1. Mobile PWA Fixed & Verified
- Fixed double-IIFE scope bug (data loader couldn't see app code)
- Fixed texts/categories not populating after bootstrap
- Verified: all 74 texts load, Genesis Hebrew interlinear works, Romans Greek works

### 2. Build System Unified
- Created `BUILD_ALL.bat` — one-click: desktop → release → mobile
- Added option [5] to `LAUNCH.bat` for Build All
- Build order enforced: `build.py` → `build.py --release` → `build_mobile.py`

### 3. Release System Reorganized (v3.3.0)
- Release now has `PC/` and `Mobile/` folders (clearly labeled)
- PC: AncientTextsStudy.html + LAUNCH.bat
- Mobile: PWA files + LAUNCH-MOBILE.bat (auto-detects local IP)
- README.txt documents both versions
- Old releases (v3.1.0, v3.1.1, v3.2.0) deleted
- Current: v3.3.0, ZIP ~40 MB

### 4. HAI Linguistics Enhancement
- Created `policy/linguistics.yaml` — full Hebrew alphabet, gematria, theophoric elements,
  name change theology, root families, number symbolism, analysis framework
- Updated `Modelfile` system prompt with linguistics expertise
- **Linguistics baked into ALL modes by default** (user requested: "it should auto-know")
- NOT a separate selectable mode — removed from MODES dict
- Core identity + linguistics sections always active in general, bible_study, berean

### 5. Files Changed
- `pipeline/bible_bound.py` — linguistics in all modes, removed linguistics mode
- `policy/linguistics.yaml` — NEW reference data file
- `Modelfile` — updated system prompt
- `build_mobile.py` — IIFE scope fix, texts/categories fix
- `release.py` — PC/ + Mobile/ folder structure, auto-IP launcher
- `BUILD_ALL.bat` — NEW one-click build script
- `LAUNCH.bat` — added Build All option
- `output/mobile/LAUNCH-MOBILE.bat` — NEW standalone mobile launcher

## Current State
- Version: 3.3.0
- All builds working (desktop, release, mobile)
- HAI has linguistics in all modes
- Need to rebuild Hallelujah model: `ollama create hallelujah -f Modelfile`
