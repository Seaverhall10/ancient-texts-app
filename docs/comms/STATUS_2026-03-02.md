# COMMS — Status Update: 2026-03-02

**From**: Hallelujah AI Session
**To**: Ancient Texts Content Session
**Priority**: READ BEFORE TOUCHING SHARED FILES

---

## What Was Done

The Hallelujah AI local chat feature has been fully built and verified live.

### New Directories (DO NOT DELETE)
```
server/          — FastAPI backend (port 8741)
  ├── __init__.py
  ├── main.py           — Routes: /health, /models, /chat, /chat/stream, /policy
  ├── config.py         — Config (localhost only, port 8741)
  └── providers/
      ├── __init__.py
      ├── ollama.py     — Ollama REST adapter (streaming)
      └── llamacpp.py   — Future stub

pipeline/        — Bible-bound processing
  ├── __init__.py
  ├── bible_bound.py    — System prompt builder (loads policy YAMLs)
  ├── verifier.py       — A/B/C/U label checker + audit trail
  └── context_builder.py — Study context formatter

policy/          — Transparent guidelines (ALL editable)
  ├── bible_bound.yaml     — Hermeneutic principles
  ├── claim_labels.yaml    — A/B/C/U definitions
  ├── response_format.yaml — Required response sections
  ├── boundaries.yaml      — Behavioral boundaries
  ├── security.yaml        — Creator protection + privacy
  └── README.md            — How to edit

tests/           — Test suite
  ├── test_health.py    — Server health (2 tests)
  ├── test_chat.py      — Chat endpoints (2 tests)
  └── test_pipeline.py  — Pipeline logic (5 tests, all passing)

COMMS/           — This folder (cross-session coordination)
```

### New Top-Level Files
```
LAUNCH_AI.bat               — One-click startup
HALLELUJAH_AI_INTEL.md      — System state, architecture, inventory
HALLELUJAH_AI_VISION.md     — Full feature vision + modes
HALLELUJAH_AI_ROADMAP.md    — Short/medium/long term plans
HALLELUJAH_AI_PROTOCOLS.md  — Testing, expansion, operational protocols
```

### Modified Files (CAUTION)

**app.py** — MAJOR CHANGE
- Added ~60 lines: backend server startup in background thread
- Server starts BEFORE PyWebView window opens
- Clean shutdown on window close
- If backend deps not installed, logs warning but still opens app

**src/js/app.js** — ADDITIVE CHANGE ONLY
- Added `initHallelujahChat()` call inside the existing `init()` function (1 line)
- Added ~450 lines BEFORE the closing `})();` of the IIFE
- The new code is a self-contained chat module
- DOES NOT modify any existing functions or variables
- Safe zones: everything from line 1 to line ~4600 is UNTOUCHED

**src/css/styles.css** — ADDITIVE CHANGE ONLY
- Added ~400 lines at the VERY END of the file
- All new classes prefixed with `hai-` (hallelujah AI)
- DOES NOT modify any existing styles
- Uses existing CSS variables (--gold, --bg-card, etc.)

**requirements.txt** — 4 lines added
```
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
pyyaml>=6.0
```

---

## System Prerequisites Installed

| Tool | Version | Location |
|------|---------|----------|
| Python | 3.12.10 | `C:\Users\User\AppData\Local\Programs\Python\Python312\` |
| Ollama | 0.17.4 | `C:\Users\User\AppData\Local\Programs\Ollama\` |
| Mistral 7B | latest | 4.4 GB model, loaded in Ollama |
| FastAPI | 0.133.1 | pip installed |
| Uvicorn | 0.41.0 | pip installed |
| HTTPX | 0.28.1 | pip installed |
| PyYAML | 6.0.3 | pip installed |
| PyWebView | 6.1 | pip installed |

---

## Current Server Status

- FastAPI: RUNNING on 127.0.0.1:8741
- Ollama: RUNNING on localhost:11434
- Model: mistral:latest (connected)
- Pipeline tests: ALL PASSING (7/7)
- Live chat test: SUCCESSFUL

---

## Action Items for Ancient Texts Session

1. **Do NOT overwrite** app.py, app.js, or styles.css without checking this COMMS folder first
2. **Safe to modify**: All data/ files, manifest.json, build.py internals, any content generation
3. **After content changes**: Run `python build.py` to rebuild HTML (will include the chat code)
4. **If you need to modify app.js**: Only touch lines 1-4600. Lines 4601+ are the chat module.
5. **If you need to modify styles.css**: Only touch lines 1-5539. Lines 5540+ are chat styles.

---

## Important Notes

- Python is NOT in the system PATH. Use full path: `C:\Users\User\AppData\Local\Programs\Python\Python312\python.exe`
- Ollama runs as a background service. It may already be running when you start.
- The chat feature is OPTIONAL — if Ollama/server isn't running, the app works normally without it
- LAUNCH_AI.bat handles everything automatically

---

**End of status update.**
