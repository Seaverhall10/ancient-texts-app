# Session Update — March 8, 2026

## What Was Done This Session

### 1. Deployed Mobile PWA to GitHub Pages
- Created repo: `Seaverhall10/ancient-texts-app` (public)
- URL: **https://seaverhall10.github.io/ancient-texts-app/**
- Authenticated `gh` CLI as Seaverhall10
- Generated PNG icons (were SVG-only, which broke on phones)

### 2. Major Mobile Navigation Overhaul
All fixes made permanent in `build_mobile.py` (survive rebuilds):

**Critical Bug Fixes:**
- `const` → `var` for all interlinear declarations (data was silently failing to load via `eval()`)
- Removed broken `pushState` function wrappers that killed all navigation clicks
- Fixed Home button to actually call `showLibrary()` instead of just `scrollTo(0,0)`
- Fixed search results to close sidebar on selection

**New Features:**
- Bottom navigation bar: Library | Search | Reader | Glossary | Saved
- Sidebar close button (X) with proper z-index (was behind its own backdrop)
- Scroll lock when sidebar is open
- Browser back button support via popstate listener

**CSS Mobile Overhaul:**
- `--content-max: min(1100px, 100%)` (was `clamp(640px,...)` causing horizontal scroll)
- Touch targets 44px minimum on all buttons/links
- `touch-action: manipulation` for instant taps
- `100dvh` fallbacks for mobile browser chrome
- 375px and 480px small phone breakpoints
- Safe area insets for notched phones
- Hover-only elements (copy buttons) now visible on touch
- `overflow-wrap: break-word` globally
- Removed `user-scalable=no` (accessibility)

### 3. Fixed Heavenly Court Content
- `build_mobile.py` now properly handles `content_type: "html_fragment"` eras
- Was dumping all 44 chapters into every era (15MB duplicated)
- Now correctly maps per-era fragments from manifest (1.6MB, proper chapters per era)

### 4. Fixed Service Worker Caching
- Changed from cache-first to **network-first** for ALL requests
- Phones now always get fresh content (fall back to cache only offline)
- Updated in both `output/mobile/sw.js` and `build_mobile.py`

## Files Modified
- `build_mobile.py` — era loading fix, mobile CSS/JS patches, SW fix
- `output/mobile/index.html` — rebuilt with all fixes
- `output/mobile/sw.js` — network-first strategy, v3.3.3
- `output/mobile/icon-180.png`, `icon-192.png`, `icon-512.png` — new PNG icons
- `output/mobile/data/eras/heavenly_court.json` — proper per-era content

## Current State
- GitHub Pages live at: https://seaverhall10.github.io/ancient-texts-app/
- All fixes permanent in build_mobile.py
- User reports phone still showing old cached version — needs to clear site data

## What's Still Needed
1. **Phone cache clear** — user needs to clear seaverhall10.github.io site data
2. **Real phone testing** — verify bottom nav, sidebar, navigation all work after cache clear
3. **Further mobile polish** — reading pane header too crowded, word popup could be bottom sheet
4. **NT era depth** — most NT books still have only 1 era
